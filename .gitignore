# Credenciales y configuración sensible
.env
*.env
.env.*

# API Keys y tokens (si están en archivos separados)
config/api_keys.py
secrets.py
credentials.json

# Entornos virtuales
venv/
env/
.venv/
.env/
ENV/

# Archivos compilados Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# IDE y editores
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Configuración local Claude Code (el settings.json compartido, commands/ y hooks/ SÍ se versionan)
.claude/*
!.claude/settings.json
!.claude/commands/
!.claude/hooks/

# Logs temporales y archivos de debugging
*.log
debug.txt
temp/
tmp/
# Artefacto de los hooks en Windows: el runner materializa >nul como ARCHIVO vacío en el cwd, se recrea por turno (15-jul-2026)
nul
# log de runtime del especialista (no versionar) — comentario en línea propia: inline rompe el patrón
error_esp.txt

# Logs grandes de desarrollo histórico (>50MB)
logs_simulador/log_scans_2026-W12.txt
historial/logs_desarrollo/log_sniper_sim.txt

# Datos pesados que NO van al repo (GitHub rechaza >100MB por archivo)
analisis_VPS_2026-06-04/
analisis/datasets/